<div class="row">
<div class="col-md-5">

    <div class="ui-dform-div form-group">
        <label id="JPKIE_${field.name}" for="jpkieformparam_${field.name}" class="editLabel">$i18n.getLabel("JPKIE_${field.name}")</label>
        <input type="${field.typeHTML}" id="${field.id}" name="${field.name}" labelkey="JPKIE_${field.name}" class="form-control ui-widget ui-dform-number" aria-required="true" value="">
    </div>

</div>
</div>